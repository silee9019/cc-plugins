{
  "description": "Cross-project skill/command cache on session start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/sync.py"
          }
        ]
      }
    ]
  }
}
